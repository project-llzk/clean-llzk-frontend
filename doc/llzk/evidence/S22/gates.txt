# S22 — R5 X1: confine the unchecked lookup-table path

Date (UTC): 2026-08-01T21:41:31Z

## Gates

$ LLZK_OPT=... LLZK_WITGEN=... bash scripts/llzk/e2e.sh
PASS: G0 G1 G2 G3 G4 G5 G6 G7 G8 G9 G10 G11 G12
exit status: 0

## Attribution

The caveat this file originally carried — that the gate run included another
session's uncommitted X2 work — has been discharged. X2 landed as 5b8b0b52 and
the run above was repeated on the resulting clean tree:

  PASS: G0 G1 G2 G3 G4 G5 G6 G7 G8 G9 G10 G11 G12   exit 0
  11 circuits, 30 input vectors, both witgen backends

But 5b8b0b52 is titled "X2: stop refusing correct modules for circuits that copy
a variable" and its message describes only X2. It also contains, because that
session staged the whole tree, all of S22's X1 work: the private Config
constructor, Config.unsafeWithTables, the ofCertified rewiring, the withdrawn
ConstraintSet.globals claim, the fatBytes controls, D022, G12 and
scripts/llzk/check-unsafe-config.sh. It also contains R5-findings.md.

Nothing was lost and the gates are green. What is wrong is the history: a
reviewer reading 5b8b0b52 would not learn that G12 and D022 landed in it. This
file is the attribution record. The history is deliberately not rewritten —
doing so under a session that was still active would have been worse.

## G12, and its falsifiability

$ bash scripts/llzk/check-unsafe-config.sh
unsafe-config check: no non-test module names Config.unsafeWithTables
exit 0

Planting the identifier in Clean/Backend/LLZK/Corpus.lean:
  error: Config.unsafeWithTables outside a test module:
    Clean/Backend/LLZK/Corpus.lean                        exit 1
Removing it returned exit 0.

## What the new controls pin

Test/Constraints.lean now compiles WITH R5's bad config rather than
comparing two configs production never pairs:

  fatBytes = 512 rows where Gadgets.ByteTable has 256
  compileSource fatBytes addSrc  -> .ok, and agree fatBytes addSrc m
     holds, because both sides read cfg.tables. That is the tautology
     R5 identified, now pinned rather than claimed against.
  the emitted @Bytes global contains 300, which ByteTable does not.

Both are #guards, so if either stops being true the build fails.
